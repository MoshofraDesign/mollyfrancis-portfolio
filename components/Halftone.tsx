// Unused — superseded by the CSS-only `.halftone-sweep` hover effect
// (app/globals.css) applied directly to the wrapping div around the
// next/image halftone in RotatingHero.tsx. That approach animates a
// diagonal mask over the existing SVG asset with no DOM/markup changes,
// so this per-dot-inlining component was never wired up. Left as an inert
// stub rather than deleted (file deletion isn't available in this
// environment) — safe to remove by hand if you want it gone.
export {};
